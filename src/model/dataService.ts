import {WeekSummary} from "./WeekSummary";
import {Activity} from "./Activity";

export const getWeekSummaries = (data: Array<Activity>): Array<WeekSummary> => {
  // partition data into weeks first
  // then apply makeWeekSummary to each week
  return [{},{}];
}