import React, {FunctionComponent} from "react";

interface Props {
  data: any; // TODO proper report definition
}

const TimelineView: FunctionComponent<Props> = ({data}) => {
  return (
    <div>timeline</div>
  )
};

export default TimelineView;